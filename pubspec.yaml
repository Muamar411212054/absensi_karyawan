name: absensi_karyawan
description: "Sistem Absensi Selfie dan Lokasi"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  geolocator: ^13.0.1
  camera: ^0.11.0
  permission_handler: ^11.3.1
  intl: ^0.20.2
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter             : 
uses-material-design: true
